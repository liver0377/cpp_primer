#include <cmath>
#include <iomanip>
#include <iostream>

using namespace std;

int main() {
    cout << hexfloat << uppercase;
    cout << 100 * sqrt(2.0) << endl;
    cout << defaultfloat << nouppercase;
    cout << 100 * sqrt(2.0) << endl;
}